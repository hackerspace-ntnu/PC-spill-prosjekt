namespace GlobalEnums
{
    public enum Direction
    {
        LEFT = -1,
        RIGHT = 1,
    }

    public enum SquareSide
    {
        TOP,
        BOTTOM,
        LEFT,
        RIGHT,
    }
}
